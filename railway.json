{
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "python app.py",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5,
    "volumes": {
      "/app/ai_memory": "ai_memory_persistent"
    }
  }
}
